
  # Personal portfolio website

  This a personal portfolio web created using AI. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
